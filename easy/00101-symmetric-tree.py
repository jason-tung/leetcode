# https://leetcode.com/problems/symmetric-tree/submissions/1142803157/
        return True
    if not l and not r:
def isSymEqual(l,r):
    return l and not r or not l and r
def xor(l,r):
#         self.right = right
    if xor(l,r):
        return False
    return l.val == r.val and isSymEqual(l.left, r.right) and isSymEqual(l.right, r.
left)
class Solution: